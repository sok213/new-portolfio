import React, { Component } from 'react';
import s from './Footer.module.scss';

export default function Footer() {

  return (
    <section className={s['footer-container']}>
      <p>Footer goes here.</p>
    </section>
  );
}